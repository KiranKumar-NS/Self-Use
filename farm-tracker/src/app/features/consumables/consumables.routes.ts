import { Routes } from '@angular/router';
import { ConsumableListComponent } from './consumable-list/consumable-list.component';

export const CONSUMABLE_ROUTES: Routes = [{ path: '', component: ConsumableListComponent }];
